import random
import string

"""
Zufallsgenerator für Passwort

Deine Aufgabe ist es, ein durch Zufall generiertes Passwort zu erstellen.
Dazu brauchst du die string-Bibliothek, welche oben mit "import string" importiert wurde.

Hilfe:

String-Library Documentation
https://docs-python-org.translate.goog/3/library/string.html?_x_tr_sl=en&_x_tr_tl=de&_x_tr_hl=it&_x_tr_pto=op,sc

Suche die Befehle für Klein - und Großbuchstaben, Ziffern und Sonderzeichen in der Documentation
"""

#Verwende die vorhin gesuchten Befehle, um die Werte in 4 Variablen zu speichern.

#Verwende die String-Addition, um alle 4 Variablen zu einer zusammenzufügen

#Input, wie lang das Passwort sein soll + Umwandlung in int, da inputs als String zurückgegeben werden.
lenght = int(input("Wie lang soll das Passwort sein? : "))

#Die Varaible "password" als leerer String kreieren
password = ""

#Kreiere eine for-Schleife nach folgendem Prinzip:
#Für eine gewisse Anzahl an Durchläufen, welche der Länge des Passworts entspricht:
#     Speicher mithilfe von random.choice(Variable mit allen Möglichkeiten) einen zufälligen Wert in eine Variable
#     Erweiter die Variable password(wie bei a = a + 1) mit dem zufällig gewählten Wert


#gib die Variable password aus

